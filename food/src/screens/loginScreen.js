import React from "react";
import { View, Text, StyleSheet } from "react-native";
import SearchBar from "../components/SearchBar";
const LoginScreen = () => {
  return (
    <View>
      <SearchBar>Search Screen</SearchBar>
    </View>
  );
};

export default LoginScreen;
